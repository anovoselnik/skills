interface:
  display_name: "Shipmate"
  short_description: "Implement work, open its PR, and babysit it"
  default_prompt: "Use $shipmate to implement this task, open its pull request, and babysit it until it is merge-ready."
policy:
  allow_implicit_invocation: true
