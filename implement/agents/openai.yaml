interface:
  display_name: "Implement"
  short_description: "Build, verify, review, and commit a defined task"
  default_prompt: "Use $implement to build, verify, review, and commit the supplied task."
policy:
  allow_implicit_invocation: true
